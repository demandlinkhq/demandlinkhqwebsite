I am building a website in GoHighLevel (GHL) using only custom code blocks (HTML, CSS, JS inside each block (header block, content block, footer block). 
I do not want to use page-level CSS, so all styling should be scoped within each code block. 
The containers of the blocks will have all their margins and paddings set to zero on GHL, allowing the code to completely determine the design.
